<div id="target">
    <span id="date">
        <b>@Today.ToString("%y-%M-%d %h:%m:%s")</b><button data-on-click="@Get(new RemoveDateEndpoint())">Remove</button>
    </span>
</div>

@code
{
    [Parameter] [EditorRequired] public required DateTime Today { get; set; }
}
