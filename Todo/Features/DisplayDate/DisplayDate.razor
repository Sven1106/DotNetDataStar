<form id="target">
    <span id="date">
        <b>@Today.ToString("%y-%M-%d %h:%m:%s")</b><button data-on:click=@("@post('/removeDate')")>Remove</button>
    </span>
</form>

@code
{
    [Parameter] [EditorRequired] public required DateTime Today { get; set; }
}
